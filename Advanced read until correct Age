#include <iostream>
#include <string>
using namespace std;
int readAge()
{
	int Age;
	cout << "please enter your correct Age :"; cin >> Age;
	return Age;
}
bool ValidateAgeInRange(int Age, int From, int To)
{
	return (Age >= From && Age <= To);
}
int ReadUntilAgeBetween(int From, int To)
{
	int Age = 0;
	do {
		Age = readAge();
		ValidateAgeInRange(Age, From, To);
		if (Age < To) {
			cout << endl << "you are child,";}
		if (Age > From) {
			cout << endl << "Are you from the age of dinosaurs?,";}
	} while (!(ValidateAgeInRange(Age, From, To)));
	return Age;
}
void PrintResult(int Age)
{
		cout << "you are valid to company" << endl;
}
int main()
{
	PrintResult(ReadUntilAgeBetween(18, 45));
	return 0;
}
